metadata:
  config_version: 1
  description: "V2 context source configuration."

structured_sources:
  eia:
    enabled: false
    api_key_env: EIA_API_KEY
    purpose: "Oil inventory and release-window context."
    feeds_memory_cache: true
    writes_questdb_ledger: true
    used_in_per_tick_loop: false
    max_staleness_seconds: 86400

  fred:
    enabled: false
    api_key_env: FRED_API_KEY
    purpose: "Macro/rates context."
    feeds_memory_cache: true
    writes_questdb_ledger: true
    used_in_per_tick_loop: false
    max_staleness_seconds: 86400

  usaspending:
    enabled: false
    api_key_required: false
    purpose: "Defense contract award context."
    feeds_memory_cache: true
    writes_questdb_ledger: true
    used_in_per_tick_loop: false
    max_staleness_seconds: 86400

  yfinance_dev_only:
    enabled: false
    development_only: true
    production_critical: false
    required: false
    purpose: "Development-only sector/index proxy context."
    feeds_memory_cache: true
    writes_questdb_ledger: true
    used_in_per_tick_loop: false
    period: "5d"
    interval: "5m"
    timeout_seconds: 10.0
    bar_completion_grace_seconds: 30
    max_staleness_seconds: 360
    auto_adjust: false
    actions: false
    repair: false
    keepna: true
    prepost: false
    threads: true

unstructured_sources:
  sec_edgar:
    enabled: false
    user_agent_env: SEC_USER_AGENT
    purpose: "SEC filing text and metadata for AI context flags."
    feeds_ai_context_filter: true
    direct_trade_authority: false

  news:
    enabled: false
    purpose: "News text for AI context flags."
    feeds_ai_context_filter: true
    direct_trade_authority: false

  social:
    enabled: false
    purpose: "Social/political posts for AI context flags."
    feeds_ai_context_filter: true
    direct_trade_authority: false

ai_context_filter:
  enabled: false
  direct_trade_authority: false
  required_output_schema: context_flag_v1
  prompt_version: context_filter_v1
  temperature: 0
  require_valid_until: true
