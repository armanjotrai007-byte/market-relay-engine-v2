# Placeholder paper-trading limits for PR 1.
# These are not optimized live-trading settings.
trading_mode: paper

limits:
  max_spread_bps: 10
  max_latency_ms: 1000
  max_daily_loss_usd: 100
  max_open_positions: 3
  default_trade_size_usd: 1000

notes:
  live_trading_enabled: false
  requires_future_validation: true
